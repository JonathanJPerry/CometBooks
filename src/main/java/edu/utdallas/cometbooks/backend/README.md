# edu.utdallas.cometbooks.backend package

This hosts just the backend components of the system, which were made with our use case realization diagram and other
diagrams as well. All communication from the frontend goes through the `Controller`.